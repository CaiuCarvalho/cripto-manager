# AgonCripto Frontend — Testing Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Instalar Vitest + Testing Library, escrever e rodar testes unitários de formatters, integridade de dados, componentes UI e smoke tests das 4 páginas, e adicionar pre-commit hook.

**Architecture:** Vitest roda em ambiente jsdom dentro de `frontend/`. Cada layer tem seu próprio arquivo de teste em `src/__tests__/`. As páginas são renderizadas diretamente como componentes React; CSS variables não resolvem em jsdom mas os testes só verificam texto/estrutura.

**Tech Stack:** Vitest 1.x, @testing-library/react 14.x, @testing-library/jest-dom 6.x, @testing-library/user-event 14.x, jsdom, @vitejs/plugin-react

---

## Arquivos que serão criados/modificados

| Ação | Arquivo |
|---|---|
| Create | `frontend/vitest.config.ts` |
| Create | `frontend/vitest.setup.ts` |
| Modify | `frontend/package.json` — adicionar scripts `test` e `test:run` |
| Create | `frontend/src/__tests__/lib/formatters.test.ts` |
| Create | `frontend/src/__tests__/lib/mock-data.test.ts` |
| Create | `frontend/src/__tests__/components/pill.test.tsx` |
| Create | `frontend/src/__tests__/components/coin-mark.test.tsx` |
| Create | `frontend/src/__tests__/components/sparkline.test.tsx` |
| Create | `frontend/src/__tests__/components/allocation-donut.test.tsx` |
| Create | `frontend/src/__tests__/pages/dashboard.test.tsx` |
| Create | `frontend/src/__tests__/pages/wallets.test.tsx` |
| Create | `frontend/src/__tests__/pages/transactions.test.tsx` |
| Create | `frontend/src/__tests__/pages/alerts.test.tsx` |
| Create | `.git/hooks/pre-commit` |

---

## Task 1: Instalar dependências e configurar Vitest

**Files:**
- Modify: `frontend/package.json`
- Create: `frontend/vitest.config.ts`
- Create: `frontend/vitest.setup.ts`

- [ ] **Step 1: Instalar pacotes**

```bash
cd frontend
npm install -D vitest @vitest/coverage-v8 @testing-library/react @testing-library/jest-dom @testing-library/user-event @vitejs/plugin-react jsdom
```

Esperado: instalação sem erros, `package.json` atualizado com os devDependencies.

- [ ] **Step 2: Criar `frontend/vitest.config.ts`**

```ts
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./vitest.setup.ts'],
    include: ['src/__tests__/**/*.{test,spec}.{ts,tsx}'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

- [ ] **Step 3: Criar `frontend/vitest.setup.ts`**

```ts
import '@testing-library/jest-dom';
```

- [ ] **Step 4: Adicionar scripts em `frontend/package.json`**

Localizar o bloco `"scripts"` existente e substituí-lo por:

```json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "test": "vitest",
  "test:run": "vitest run",
  "test:cover": "vitest run --coverage"
},
```

- [ ] **Step 5: Verificar que o runner sobe sem erros**

```bash
cd frontend && npm run test:run -- --reporter=verbose 2>&1 | head -20
```

Esperado: `No test files found` (sem erros de configuração).

- [ ] **Step 6: Commit**

```bash
cd .. && git add frontend/package.json frontend/package-lock.json frontend/vitest.config.ts frontend/vitest.setup.ts
git commit -m "chore: install and configure Vitest + Testing Library"
```

---

## Task 2: Testes de formatters

**Files:**
- Create: `frontend/src/__tests__/lib/formatters.test.ts`

- [ ] **Step 1: Criar o arquivo de testes**

```ts
import { describe, it, expect } from 'vitest';
import {
  fmtBRL, fmtPct, fmtAmount, fmtDate, fmtAbrev, fmtRelative, shortAddr,
} from '@/lib/formatters';

describe('fmtBRL', () => {
  it('formata valor positivo em BRL', () => {
    expect(fmtBRL(1000)).toBe('R$ 1.000,00');
  });
  it('oculta valor quando hide=true', () => {
    expect(fmtBRL(99999, { hide: true })).toBe('R$ •••••');
  });
  it('respeita decimals customizado', () => {
    expect(fmtBRL(1.5, { decimals: 0 })).toBe('R$ 2');
  });
});

describe('fmtPct', () => {
  it('valor positivo tem prefixo +', () => {
    expect(fmtPct(1.42)).toBe('+1.42%');
  });
  it('valor negativo não tem prefixo +', () => {
    expect(fmtPct(-2.14)).toBe('-2.14%');
  });
  it('zero tem prefixo +', () => {
    expect(fmtPct(0)).toBe('+0.00%');
  });
});

describe('fmtAmount', () => {
  it('formata inteiro sem casas decimais desnecessárias', () => {
    expect(fmtAmount(4200, 4)).toBe('4.200');
  });
  it('formata decimal com precisão correta', () => {
    expect(fmtAmount(0.042, 4)).toBe('0,042');
  });
});

describe('fmtDate', () => {
  it('retorna string não vazia para ISO válido', () => {
    const result = fmtDate('2026-04-26T14:32:00');
    expect(result).toBeTruthy();
    expect(result).toContain('·');
  });
});

describe('fmtAbrev', () => {
  it('formata milhões', () => {
    expect(fmtAbrev(2_500_000)).toBe('2.5M');
  });
  it('formata milhares', () => {
    expect(fmtAbrev(1500)).toBe('1.5k');
  });
  it('retorna número bruto abaixo de 1000', () => {
    expect(fmtAbrev(42)).toBe('42');
  });
});

describe('fmtRelative', () => {
  it('retorna "agora" para data muito recente', () => {
    const now = new Date().toISOString();
    expect(fmtRelative(now)).toBe('agora');
  });
  it('retorna string com "atrás" para data passada', () => {
    const old = new Date(Date.now() - 2 * 60 * 60 * 1000).toISOString();
    expect(fmtRelative(old)).toContain('atrás');
  });
});

describe('shortAddr', () => {
  it('abrevia endereço longo', () => {
    const addr = 'bc1qxy2kgdygjrsqtzq2n0yrf2493p83kkfjhx0wlh';
    expect(shortAddr(addr)).toBe('bc1qxy…x0wlh');
  });
  it('retorna endereço mascarado sem modificação', () => {
    expect(shortAddr('••••3829')).toBe('••••3829');
  });
});
```

- [ ] **Step 2: Rodar os testes**

```bash
cd frontend && npm run test:run -- --reporter=verbose src/__tests__/lib/formatters.test.ts
```

Esperado: todos os testes passam (verde).

- [ ] **Step 3: Commit**

```bash
cd .. && git add frontend/src/__tests__/lib/formatters.test.ts
git commit -m "test: add formatter unit tests"
```

---

## Task 3: Testes de integridade do mock-data

**Files:**
- Create: `frontend/src/__tests__/lib/mock-data.test.ts`

- [ ] **Step 1: Criar o arquivo de testes**

```ts
import { describe, it, expect } from 'vitest';
import {
  COINS, HOLDINGS, TRANSACTIONS, WALLETS, TX_EXTENDED, ALERTS,
  NETWORKS, VENUES,
} from '@/lib/mock-data';

describe('COINS', () => {
  it('tem pelo menos 4 moedas', () => {
    expect(COINS.length).toBeGreaterThanOrEqual(4);
  });
  it('cada moeda tem campos obrigatórios', () => {
    COINS.forEach(c => {
      expect(c.id).toBeTruthy();
      expect(c.symbol).toBeTruthy();
      expect(c.price).toBeGreaterThan(0);
      expect(c.color).toMatch(/^#[0-9a-f]{6}$/i);
    });
  });
  it('IDs são únicos', () => {
    const ids = COINS.map(c => c.id);
    expect(new Set(ids).size).toBe(ids.length);
  });
});

describe('HOLDINGS', () => {
  it('cada holding referencia uma moeda existente', () => {
    const coinIds = new Set(COINS.map(c => c.id));
    HOLDINGS.forEach(h => {
      expect(coinIds.has(h.coin)).toBe(true);
    });
  });
  it('amount e costBasis são positivos', () => {
    HOLDINGS.forEach(h => {
      expect(h.amount).toBeGreaterThan(0);
      expect(h.costBasis).toBeGreaterThan(0);
    });
  });
});

describe('TRANSACTIONS', () => {
  it('type é buy ou sell', () => {
    TRANSACTIONS.forEach(t => {
      expect(['buy', 'sell']).toContain(t.type);
    });
  });
  it('IDs são únicos', () => {
    const ids = TRANSACTIONS.map(t => t.id);
    expect(new Set(ids).size).toBe(ids.length);
  });
});

describe('WALLETS', () => {
  it('status é ok, syncing ou error', () => {
    WALLETS.forEach(w => {
      expect(['ok', 'syncing', 'error']).toContain(w.status);
    });
  });
  it('type é onchain ou exchange', () => {
    WALLETS.forEach(w => {
      expect(['onchain', 'exchange']).toContain(w.type);
    });
  });
  it('saldo é número não-negativo', () => {
    WALLETS.forEach(w => {
      expect(w.balance_brl).toBeGreaterThanOrEqual(0);
    });
  });
  it('IDs são únicos', () => {
    const ids = WALLETS.map(w => w.id);
    expect(new Set(ids).size).toBe(ids.length);
  });
});

describe('TX_EXTENDED', () => {
  it('type é buy, sell, reward ou transfer', () => {
    TX_EXTENDED.forEach(t => {
      expect(['buy', 'sell', 'reward', 'transfer']).toContain(t.type);
    });
  });
  it('wallet referencia wallet existente', () => {
    const walletIds = new Set(WALLETS.map(w => w.id));
    TX_EXTENDED.forEach(t => {
      expect(walletIds.has(t.wallet)).toBe(true);
    });
  });
  it('IDs são únicos', () => {
    const ids = TX_EXTENDED.map(t => t.id);
    expect(new Set(ids).size).toBe(ids.length);
  });
});

describe('ALERTS', () => {
  it('status é armed, triggered ou paused', () => {
    ALERTS.forEach(a => {
      expect(['armed', 'triggered', 'paused']).toContain(a.status);
    });
  });
  it('kind é above, below, change ou wallet', () => {
    ALERTS.forEach(a => {
      expect(['above', 'below', 'change', 'wallet']).toContain(a.kind);
    });
  });
  it('IDs são únicos', () => {
    const ids = ALERTS.map(a => a.id);
    expect(new Set(ids).size).toBe(ids.length);
  });
});

describe('NETWORKS e VENUES', () => {
  it('networks têm id, label e color', () => {
    NETWORKS.forEach(n => {
      expect(n.id).toBeTruthy();
      expect(n.label).toBeTruthy();
      expect(n.color).toMatch(/^#/);
    });
  });
  it('venues têm id, label e color', () => {
    VENUES.forEach(v => {
      expect(v.id).toBeTruthy();
      expect(v.label).toBeTruthy();
      expect(v.color).toMatch(/^#/);
    });
  });
});
```

- [ ] **Step 2: Rodar os testes**

```bash
cd frontend && npm run test:run -- --reporter=verbose src/__tests__/lib/mock-data.test.ts
```

Esperado: todos passam.

- [ ] **Step 3: Commit**

```bash
cd .. && git add frontend/src/__tests__/lib/mock-data.test.ts
git commit -m "test: add mock-data integrity tests"
```

---

## Task 4: Testes do componente Pill

**Files:**
- Create: `frontend/src/__tests__/components/pill.test.tsx`

- [ ] **Step 1: Criar o arquivo de testes**

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { Pill } from '@/components/ui/pill';

describe('Pill', () => {
  it('exibe valor positivo com sinal ▲', () => {
    render(<Pill value={1.42} />);
    expect(screen.getByText(/1\.42%/)).toBeInTheDocument();
    expect(screen.getByText('▲')).toBeInTheDocument();
  });

  it('exibe valor negativo com sinal ▼', () => {
    render(<Pill value={-2.14} />);
    expect(screen.getByText(/2\.14%/)).toBeInTheDocument();
    expect(screen.getByText('▼')).toBeInTheDocument();
  });

  it('exibe zero com sinal ▲', () => {
    render(<Pill value={0} />);
    expect(screen.getByText('▲')).toBeInTheDocument();
  });

  it('exibe valor absoluto (sem sinal negativo no número)', () => {
    render(<Pill value={-5.5} />);
    expect(screen.getByText(/5\.50%/)).toBeInTheDocument();
    expect(screen.queryByText(/-5/)).toBeNull();
  });
});
```

- [ ] **Step 2: Rodar os testes**

```bash
cd frontend && npm run test:run -- --reporter=verbose src/__tests__/components/pill.test.tsx
```

Esperado: 4 testes passam.

- [ ] **Step 3: Commit**

```bash
cd .. && git add frontend/src/__tests__/components/pill.test.tsx
git commit -m "test: add Pill component tests"
```

---

## Task 5: Testes do componente CoinMark

**Files:**
- Create: `frontend/src/__tests__/components/coin-mark.test.tsx`

- [ ] **Step 1: Criar o arquivo de testes**

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { CoinMark } from '@/components/ui/coin-mark';

describe('CoinMark', () => {
  it('exibe as duas primeiras letras do símbolo', () => {
    render(<CoinMark symbol="BTC" color="#f2a23a" />);
    expect(screen.getByText('BT')).toBeInTheDocument();
  });

  it('funciona com símbolo de 2 letras', () => {
    render(<CoinMark symbol="ET" color="#7b8aff" />);
    expect(screen.getByText('ET')).toBeInTheDocument();
  });

  it('aplica a cor de fundo', () => {
    const { container } = render(<CoinMark symbol="SOL" color="#9b6bd6" />);
    const el = container.firstChild as HTMLElement;
    expect(el.style.background).toBe('rgb(155, 107, 214)');
  });

  it('tamanho padrão é 28px', () => {
    const { container } = render(<CoinMark symbol="BTC" color="#f2a23a" />);
    const el = container.firstChild as HTMLElement;
    expect(el.style.width).toBe('28px');
    expect(el.style.height).toBe('28px');
  });

  it('respeita tamanho customizado', () => {
    const { container } = render(<CoinMark symbol="BTC" color="#f2a23a" size={40} />);
    const el = container.firstChild as HTMLElement;
    expect(el.style.width).toBe('40px');
    expect(el.style.height).toBe('40px');
  });
});
```

- [ ] **Step 2: Rodar os testes**

```bash
cd frontend && npm run test:run -- --reporter=verbose src/__tests__/components/coin-mark.test.tsx
```

Esperado: 5 testes passam.

- [ ] **Step 3: Commit**

```bash
cd .. && git add frontend/src/__tests__/components/coin-mark.test.tsx
git commit -m "test: add CoinMark component tests"
```

---

## Task 6: Testes do componente Sparkline

**Files:**
- Create: `frontend/src/__tests__/components/sparkline.test.tsx`

- [ ] **Step 1: Criar o arquivo de testes**

```tsx
import { describe, it, expect } from 'vitest';
import { render, container } from '@testing-library/react';
import { Sparkline } from '@/components/ui/sparkline';

describe('Sparkline', () => {
  it('renderiza SVG com dados válidos', () => {
    const { container } = render(<Sparkline data={[1, 2, 3, 4, 5]} />);
    expect(container.querySelector('svg')).toBeInTheDocument();
    expect(container.querySelector('path')).toBeInTheDocument();
  });

  it('retorna null com menos de 2 pontos', () => {
    const { container } = render(<Sparkline data={[42]} />);
    expect(container.firstChild).toBeNull();
  });

  it('retorna null com array vazio', () => {
    const { container } = render(<Sparkline data={[]} />);
    expect(container.firstChild).toBeNull();
  });

  it('respeita width e height customizados', () => {
    const { container } = render(<Sparkline data={[1, 2, 3]} width={100} height={40} />);
    const svg = container.querySelector('svg')!;
    expect(svg.getAttribute('width')).toBe('100');
    expect(svg.getAttribute('height')).toBe('40');
  });

  it('path tem atributo d definido', () => {
    const { container } = render(<Sparkline data={[10, 20, 15, 25]} />);
    const path = container.querySelector('path')!;
    expect(path.getAttribute('d')).toBeTruthy();
    expect(path.getAttribute('d')).toContain('M');
  });
});
```

- [ ] **Step 2: Rodar os testes**

```bash
cd frontend && npm run test:run -- --reporter=verbose src/__tests__/components/sparkline.test.tsx
```

Esperado: 5 testes passam.

- [ ] **Step 3: Commit**

```bash
cd .. && git add frontend/src/__tests__/components/sparkline.test.tsx
git commit -m "test: add Sparkline component tests"
```

---

## Task 7: Testes do componente AllocationDonut

**Files:**
- Create: `frontend/src/__tests__/components/allocation-donut.test.tsx`

- [ ] **Step 1: Criar o arquivo de testes**

```tsx
import { describe, it, expect } from 'vitest';
import { render } from '@testing-library/react';
import { AllocationDonut } from '@/components/ui/allocation-donut';

const SLICES = [
  { color: '#f2a23a', value: 30000, symbol: 'BTC' },
  { color: '#7b8aff', value: 20000, symbol: 'ETH' },
  { color: '#9b6bd6', value: 10000, symbol: 'SOL' },
];

describe('AllocationDonut', () => {
  it('renderiza um SVG', () => {
    const { container } = render(<AllocationDonut slices={SLICES} />);
    expect(container.querySelector('svg')).toBeInTheDocument();
  });

  it('renderiza um circle por fatia mais o círculo de fundo', () => {
    const { container } = render(<AllocationDonut slices={SLICES} />);
    // 1 fundo + N fatias
    const circles = container.querySelectorAll('circle');
    expect(circles.length).toBe(SLICES.length + 1);
  });

  it('exibe totalLabel quando fornecido', () => {
    const { getByText } = render(
      <AllocationDonut slices={SLICES} totalLabel="R$ 60k" />
    );
    expect(getByText('R$ 60k')).toBeInTheDocument();
  });

  it('exibe texto TOTAL', () => {
    const { getByText } = render(<AllocationDonut slices={SLICES} />);
    expect(getByText('TOTAL')).toBeInTheDocument();
  });

  it('tamanho padrão é 140x140', () => {
    const { container } = render(<AllocationDonut slices={SLICES} />);
    const svg = container.querySelector('svg')!;
    expect(svg.getAttribute('width')).toBe('140');
    expect(svg.getAttribute('height')).toBe('140');
  });
});
```

- [ ] **Step 2: Rodar os testes**

```bash
cd frontend && npm run test:run -- --reporter=verbose src/__tests__/components/allocation-donut.test.tsx
```

Esperado: 5 testes passam.

- [ ] **Step 3: Commit**

```bash
cd .. && git add frontend/src/__tests__/components/allocation-donut.test.tsx
git commit -m "test: add AllocationDonut component tests"
```

---

## Task 8: Smoke test — Dashboard

**Files:**
- Create: `frontend/src/__tests__/pages/dashboard.test.tsx`

- [ ] **Step 1: Criar o arquivo de testes**

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import DashboardPage from '@/app/(dashboard)/dashboard/page';

describe('DashboardPage (smoke)', () => {
  it('renderiza sem erros', () => {
    expect(() => render(<DashboardPage />)).not.toThrow();
  });

  it('exibe o label "Patrimônio total"', () => {
    render(<DashboardPage />);
    expect(screen.getByText(/patrimônio total/i)).toBeInTheDocument();
  });

  it('exibe o label "P&L total"', () => {
    render(<DashboardPage />);
    expect(screen.getByText(/p&l total/i)).toBeInTheDocument();
  });

  it('exibe pelo menos um símbolo de moeda da carteira (BTC)', () => {
    render(<DashboardPage />);
    expect(screen.getAllByText(/BTC/i).length).toBeGreaterThan(0);
  });

  it('exibe os tabs de range', () => {
    render(<DashboardPage />);
    expect(screen.getByText('1D')).toBeInTheDocument();
    expect(screen.getByText('1M')).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Rodar os testes**

```bash
cd frontend && npm run test:run -- --reporter=verbose src/__tests__/pages/dashboard.test.tsx
```

Esperado: 5 testes passam.

- [ ] **Step 3: Commit**

```bash
cd .. && git add frontend/src/__tests__/pages/dashboard.test.tsx
git commit -m "test: add Dashboard page smoke tests"
```

---

## Task 9: Smoke test — Wallets

**Files:**
- Create: `frontend/src/__tests__/pages/wallets.test.tsx`

- [ ] **Step 1: Criar o arquivo de testes**

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import WalletsPage from '@/app/(dashboard)/wallets/page';

describe('WalletsPage (smoke)', () => {
  it('renderiza sem erros', () => {
    expect(() => render(<WalletsPage />)).not.toThrow();
  });

  it('exibe o label "Total em carteiras"', () => {
    render(<WalletsPage />);
    expect(screen.getByText(/total em carteiras/i)).toBeInTheDocument();
  });

  it('exibe o grupo On-chain', () => {
    render(<WalletsPage />);
    expect(screen.getByText(/on-chain/i)).toBeInTheDocument();
  });

  it('exibe o grupo Exchanges', () => {
    render(<WalletsPage />);
    expect(screen.getByText(/exchanges/i)).toBeInTheDocument();
  });

  it('exibe o nome de pelo menos uma wallet', () => {
    render(<WalletsPage />);
    expect(screen.getByText('Cold Storage BTC')).toBeInTheDocument();
  });

  it('exibe o botão "Adicionar carteira"', () => {
    render(<WalletsPage />);
    expect(screen.getByText(/adicionar carteira/i)).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Rodar os testes**

```bash
cd frontend && npm run test:run -- --reporter=verbose src/__tests__/pages/wallets.test.tsx
```

Esperado: 6 testes passam.

- [ ] **Step 3: Commit**

```bash
cd .. && git add frontend/src/__tests__/pages/wallets.test.tsx
git commit -m "test: add Wallets page smoke tests"
```

---

## Task 10: Smoke test — Transactions

**Files:**
- Create: `frontend/src/__tests__/pages/transactions.test.tsx`

- [ ] **Step 1: Criar o arquivo de testes**

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import TransactionsPage from '@/app/(dashboard)/transactions/page';

describe('TransactionsPage (smoke)', () => {
  it('renderiza sem erros', () => {
    expect(() => render(<TransactionsPage />)).not.toThrow();
  });

  it('exibe o label "Total comprado"', () => {
    render(<TransactionsPage />);
    expect(screen.getByText(/total comprado/i)).toBeInTheDocument();
  });

  it('exibe o label "Total vendido"', () => {
    render(<TransactionsPage />);
    expect(screen.getByText(/total vendido/i)).toBeInTheDocument();
  });

  it('exibe o filtro "Todos"', () => {
    render(<TransactionsPage />);
    expect(screen.getByText('Todos')).toBeInTheDocument();
  });

  it('exibe o filtro "Compras"', () => {
    render(<TransactionsPage />);
    expect(screen.getByText('Compras')).toBeInTheDocument();
  });

  it('exibe linhas de transação (tipo Compra ou Venda)', () => {
    render(<TransactionsPage />);
    const badges = screen.getAllByText(/compra|venda|reward|transferência/i);
    expect(badges.length).toBeGreaterThan(0);
  });

  it('filtra por Compras ao clicar no botão', async () => {
    render(<TransactionsPage />);
    const user = userEvent.setup();
    await user.click(screen.getByText('Compras'));
    // Após filtro, não deve haver badges "Venda"
    const vendas = screen.queryAllByText(/^Venda$/i);
    expect(vendas.length).toBe(0);
  });
});
```

- [ ] **Step 2: Rodar os testes**

```bash
cd frontend && npm run test:run -- --reporter=verbose src/__tests__/pages/transactions.test.tsx
```

Esperado: 7 testes passam.

- [ ] **Step 3: Commit**

```bash
cd .. && git add frontend/src/__tests__/pages/transactions.test.tsx
git commit -m "test: add Transactions page smoke tests"
```

---

## Task 11: Smoke test — Alerts

**Files:**
- Create: `frontend/src/__tests__/pages/alerts.test.tsx`

- [ ] **Step 1: Criar o arquivo de testes**

```tsx
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import AlertsPage from '@/app/(dashboard)/alerts/page';

describe('AlertsPage (smoke)', () => {
  it('renderiza sem erros', () => {
    expect(() => render(<AlertsPage />)).not.toThrow();
  });

  it('exibe o label "Alertas ativos"', () => {
    render(<AlertsPage />);
    expect(screen.getByText(/alertas ativos/i)).toBeInTheDocument();
  });

  it('exibe o botão "Novo alerta"', () => {
    render(<AlertsPage />);
    expect(screen.getByText(/novo alerta/i)).toBeInTheDocument();
  });

  it('exibe grupo "Disparados" (há alertas triggered nos dados)', () => {
    render(<AlertsPage />);
    expect(screen.getByText(/disparados/i)).toBeInTheDocument();
  });

  it('exibe grupo "Ativos" (há alertas armed nos dados)', () => {
    render(<AlertsPage />);
    expect(screen.getByText(/^ativos$/i)).toBeInTheDocument();
  });

  it('exibe botões de ação Editar e Remover', () => {
    render(<AlertsPage />);
    expect(screen.getAllByText(/editar/i).length).toBeGreaterThan(0);
    expect(screen.getAllByText(/remover/i).length).toBeGreaterThan(0);
  });
});
```

- [ ] **Step 2: Rodar os testes**

```bash
cd frontend && npm run test:run -- --reporter=verbose src/__tests__/pages/alerts.test.tsx
```

Esperado: 6 testes passam.

- [ ] **Step 3: Commit**

```bash
cd .. && git add frontend/src/__tests__/pages/alerts.test.tsx
git commit -m "test: add Alerts page smoke tests"
```

---

## Task 12: Rodar suite completa e adicionar pre-commit hook

**Files:**
- Create: `.git/hooks/pre-commit`

- [ ] **Step 1: Rodar toda a suite**

```bash
cd frontend && npm run test:run -- --reporter=verbose
```

Esperado: todos os testes passam, zero falhas.

- [ ] **Step 2: Criar o pre-commit hook**

Criar o arquivo `.git/hooks/pre-commit` (na raiz do repositório, não dentro de `frontend/`):

```sh
#!/bin/sh
set -e
echo "Running tests before commit..."
cd frontend && npm run test:run --silent
echo "All tests passed."
```

- [ ] **Step 3: Tornar o hook executável**

```bash
chmod +x .git/hooks/pre-commit
```

- [ ] **Step 4: Verificar que o hook funciona**

```bash
cd "$(git rev-parse --show-toplevel)" && .git/hooks/pre-commit
```

Esperado: mensagem `All tests passed.` e exit code 0.

- [ ] **Step 5: Commit final**

```bash
git add frontend/src/__tests__/ frontend/vitest.config.ts frontend/vitest.setup.ts frontend/package.json frontend/package-lock.json
git commit -m "test: complete test suite with pre-commit hook"
```
